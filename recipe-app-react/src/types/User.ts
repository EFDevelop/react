export interface User {
  id: string;
  favorites: string[];  // Falls die Favoriten eine Liste von IDs sind
  username: string;  // Stelle sicher, dass diese Zeile hinzugefügt wird
  email: string;
  // Weitere Eigenschaften des Benutzers
}